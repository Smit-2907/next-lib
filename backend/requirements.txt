# These are kept for reference. The authoritative dependency list is pyproject.toml.
# To install: uv sync  (from the backend directory)
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
sqlalchemy>=2.0.0
aiosqlite>=0.20.0
bcrypt>=4.0.0
python-jose[cryptography]>=3.3.0
python-multipart>=0.0.9
pydantic[email]>=2.0.0
